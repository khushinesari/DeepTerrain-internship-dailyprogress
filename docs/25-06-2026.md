# Improvemnets to the agentic_ai_1 pipeline 
## Goals to achieve:
1. strategy_iter_{iteration}.json form conversion in every script 
2. add 10m pole +tilt_deg=5 deg 
3. check with covergae metric bug5 - calculating distance from center , but an alternative required for accurate calculation (try raycasting)
4. iteration manager optimization , should even work when routes are eliminated and hit the route threshold to improve coverage and how to tackle max cameras effectively 
5. change multi-A* to generate as many as possibel number of paths from start to end points 
6. check with the min and max camera range optimization (optional) check-20m,150m,500m 
7. gemini 2.5 flash API
8. crew AI integration 
---
## Goals achieved :
1. applied the startegy_iter_{iteration}.json to be taken as input to other scripts rather than strategy.json
---
## additional improvemnets to existing pipeline 
1. terrain_model.py
- Load ground_segmented.ply and obstacle_segmented.ply.
- Build and cache the Open3D KD-tree(s).
- Expose functions such as:
    - get_camera_origin()
    - query_nearest()
    - cast_ray()
2. raycasting.py
- Generate rays based on camera position, azimuth, tilt, horizontal FoV, and vertical FoV.
- Perform KD-tree intersection tests.
- Return the visible point cloud.
3. visibility_evaluator.py
- Replace the current build_fov_mask() implementation with calls to raycasting.py.
- Convert the visible points back into BEV using your existing bev_metadata.json.
- Compute:
    - visibility mask,
    - visible route percentage,
    - terrain coverage,
    - final candidate score.
