# current changes to the pipeline 
## goals achieved 
- implemneted the iteration based json storage in every script which is contained in the agentic_ai_1 repo file
- implemented multiple paths generation using multi a* in  interactive_multi_astar.py which randomly samples 25 start and end points on the start and goal band
## proposed changes 
- usage of Farthest point sampling (FPS ) which gives better route diversity as randomly sampling 25 points would often result in clustering
- instead of fps perform uniform sampling so as to get all possible intruder planning
```
User Draws Corridor
        │
        ▼
Smooth Polyline
        │
        ▼
Sample Every 40 Pixels
        │
        ▼
Each Sample becomes Local Circular Band
        │
        ▼
Extract Walkable Pixels
        │
        ▼
A*
```
- revised pipeline architecture
```
Multi A*

↓

Terrain Intelligence

↓

summary_iter_i.json

↓

LLM Strategy

↓

Top 5 Bottlenecks

↓

Candidate Selection

↓

Raycasting (Generate exact FOV)

↓

Extract FOV Statistics

↓

Compute Route Score
Compute Coverage Score

↓

Weighted Camera Score

↓

Choose Best Camera

↓

Remove Routes

↓

Update Terrain

↓

Repeat
```
## Final Scoring Pipeline

### For every candidate camera:

1. Raycast using the camera parameters to generate the exact FOV.
2. From that FOV, compute:
        - route intersections and intercepted route fractions,
        - newly visible terrain cells,
        - overlap with existing camera coverage,
        - frontier expansion,
        - visibility confidence from the raycasting results,
        - terrain importance using the terrain intelligence map.
3. Derive:
        - route_score from intercepted route geometry,
        - coverage_score from the FOV-derived coverage metrics.

4. Apply the strategy weights from strategy_iter_i.json:

```
final_score = route_weight   × route_score + coverage_weight × coverage_score
 ```
5. Select the highest-scoring camera, update the global coverage map, remove the observed routes, regenerate the terrain summary, and continue to the next iteration.
